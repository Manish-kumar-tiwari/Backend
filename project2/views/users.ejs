<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Project-2</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-screen bg-zinc-900 h-screen">
      <div class="py-10">
        <a class="text-blue-500 px-6 text-xl" href="/">Back to Home </a>
      </div>

      <h1 class="text-4xl py-6 px-5 text-white">All Users</h1>

      <div class="gap-3 flex flex-wrap ml-4 mt-10">
        <% if(users.lenght!=0){ %> <% users.forEach((user)=>{ %>
        <div class="w-92 bg-zinc-800 h-92 rounded-md px-2 py-2">
          <div class="px-4 bg-zinc-700 h-52 w-72 rounded-md">
            <img class="h-52  rounded-md  w-72 " src="<%= user.image %>" alt="">
          </div>

          <h1 class="text-xl text-white"><%= user.name %></h1>
          <p class="text-xl text-zinc-600"><%= user.email %></p>

          <div class="mt-10 flex justify-between px-1">
            <a class="text-zinc-500 text-lg" href="/edit/<%= user._id %>">Edit User</a>
            <a class="text-red-500 text-lg" href="/delete/<%= user._id %>">Delete User</a>
          </div>
        </div>
        <% }) }else{ %>
          <h1 class="text-white text-3xl">No user Found</h1>
        <% } %>
      </div>
    </div>
  </body>
</html>
